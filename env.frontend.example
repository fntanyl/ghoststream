# GhostStream Frontend env template
#
# IMPORTANT:
# - This file is intentionally NOT named ".env.example*" because this environment blocks writing
#   ".env*" files. On your machine, you can copy/rename it to ".env" (Vite will load it).
# - These values are PUBLIC (no secrets). Never put AES/HMAC/R2/Supabase service keys in frontend.

# Base URL of Supabase Edge Functions.
# Example (hosted):
#   https://<project-ref>.functions.supabase.co
# Example (local Supabase CLI):
#   http://127.0.0.1:54321/functions/v1
VITE_FUNCTIONS_BASE_URL=

# Only for local browser testing OUTSIDE Telegram WebView.
# Paste a valid initData query string (auth_date=...&user=...&hash=...)
VITE_DEV_INIT_DATA=


