# GhostStream Supabase Edge Functions env template
#
# Copy these into Supabase Function Secrets:
# - Supabase Dashboard -> Project Settings -> Edge Functions -> Secrets
# Or via CLI: `supabase secrets set --env-file <file>`
#
# DO NOT expose these to the frontend.

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Telegram
TELEGRAM_BOT_TOKEN=
# Max initData age accepted by backend (seconds). Default in code: 86400 (24h).
TELEGRAM_INITDATA_MAX_AGE_SECONDS=86400

# Zero-knowledge metadata encryption key (AES-256-GCM), base64-encoded 32 bytes
AES_KEY_B64=

# Blind-index tag key (HMAC-SHA256), base64-encoded bytes (32+ bytes recommended)
TAG_HMAC_KEY_B64=

# Cloudflare R2 (S3 API)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_REGION=auto
# Optional: override endpoint (defaults to https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com)
R2_ENDPOINT=


