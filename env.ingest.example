# GhostStream Admin Ingestion env template (LOCAL ONLY)
#
# This file is for `ingest.py` running on an admin machine.
# It contains sensitive credentials. Keep it private and never commit real values.
#
# The script supports both:
# - AES_KEY_B64 / TAG_HMAC_KEY_B64 (preferred, shared naming with Edge Functions)
# - GS_AES_KEY_B64 / GS_TAG_HMAC_KEY_B64 (legacy aliases)

# Supabase (service role needed for inserts + RLS bypass)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Encryption keys
AES_KEY_B64=
TAG_HMAC_KEY_B64=

# (Optional aliases)
GS_AES_KEY_B64=
GS_TAG_HMAC_KEY_B64=

# Cloudflare R2 S3 credentials
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_REGION=auto
R2_BUCKET=


